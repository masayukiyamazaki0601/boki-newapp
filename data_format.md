# 簿記3級仕訳クイズ アプリケーション問題データ入稿仕様書

本アプリケーション（QuestLearn 簿記3級仕訳ゲーム）に新規の問題レベルを追加、または既存のデータを編集する際のデータ入稿フォーマットの仕様です。

---

## 1. データファイル場所
問題データは以下のファイル内に配列構造として直接定義されています。
- **ファイルパス**: `app.js`
- **変数名**: `roadmapLevels`

---

## 2. 入稿用 JSON テンプレート

問題は以下の構造（JSONオブジェクト）に従って作成してください。

```json
{
  "id": "lvl_1",
  "level": 1,
  "title": "現金と普通預金",
  "subtitle": "すべての取引の基本となる「資産」のルールを習得します。",
  "url": "http://localhost:3001/guides/cash-and-deposit",
  "tags": ["資産", "現金", "普通預金"],
  "questions": [
    {
      "text": "【現金預入の仕訳】現金 10,000円を普通預金口座に預け入れた。正しい仕訳を選びなさい。",
      "choices": [
        "(借) 普通預金 10,000 / (貸) 現金 10,000",
        "(借) 現金 10,000 / (貸) 普通預金 10,000",
        "(借) 当座預金 10,000 / (貸) 現金 10,000",
        "(借) 普通預金 10,000 / (貸) 売上 10,000"
      ],
      "correct": 0,
      "explanation": {
        "concept": "資産の振替",
        "brilliantExplanation": "普通預金（資産）が増加したため借方に、現金（資産）が減少したため貸方に記録します。<div class='mt-2 font-bold text-emerald-600'>※解説文にはHTMLタグを用いて自由に装飾・レイアウトを組むことができます。</div>"
      }
    }
  ]
}
```

---

## 3. 各項目の仕様

| 項目名 | データ型 | 必須 / 任意 | 説明 |
| :--- | :--- | :--- | :--- |
| `id` | 文字列 | **必須** | レベルを一意に識別するための内部キー（例: `"lvl_1"`, `"lvl_2"`） |
| `level` | 数値 | **必須** | すごろくマップに表示されるステージ番号（例: `1`） |
| `title` | 文字列 | **必須** | レベルのメインタイトル。すごろくのピンの下に表示されます。 |
| `subtitle` | 文字列 | **必須** | レベル詳細ポップアップおよび学習ダッシュボードに表示される概要説明文。 |
| `url` | 文字列 | **必須** | 「解説を開く」ボタンクリック時の遷移先ガイドページのURL。 |
| `tags` | 配列 (文字列) | **必須** | レベルに関連するハッシュタグのリスト（`#`記号は不要）。 |
| `questions` | 配列 (オブジェクト)| **必須** | このレベルで出題されるクイズ問題のリスト（1つのレベルに複数設定可能）。 |
| `questions.text` | 文字列 | **必須** | クイズの問題文。 |
| `questions.choices` | 配列 (文字列) | **必須** | 選択肢リスト。**2択**（借方/貸方など）または**4択**に対応しています。 |
| `questions.correct` | 数値 | **必須** | 正解の選択肢のインデックス（`0`が1つ目の選択肢、`1`が2つ目の選択肢を指します）。 |
| `questions.explanation`| オブジェクト | **必須** | 回答後に表示されるBrilliant風の解説コンテンツ。 |
| `explanation.concept` | 文字列 | **必須** | 解説の一番上に表示されるキーコンセプト名。 |
| `explanation.brilliantExplanation`| 文字列 | **必須** | 構造化された視覚的解説文（HTMLタグを用いたグリッドレイアウトなどの装飾が可能）。 |

---

## 4. 大量作成時の推奨フロー（スプレッドシート運用）
問題の作成・校正は、表計算ソフト（GoogleスプレッドシートやExcelなど）で行い、最終的にJSONへ変換する手法が推奨されます。

### 推奨列レイアウト
1. `A列 (id)`
2. `B列 (level)`
3. `C列 (title)`
4. `D列 (subtitle)`
5. `E列 (url)`
6. `F列 (tags)`（カンマ区切り）
7. `G列 (question_text)`
8. `H列 (choice_1)`
9. `I列 (choice_2)`
10. `J列 (choice_3)`
11. `K列 (choice_4)`
12. `L列 (correct_index)`（0〜3）
13. `M列 (concept)`
14. `N列 (brilliantExplanation)`（HTML文字列）
